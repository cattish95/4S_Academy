<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:id="@+id/popup_layout"
        android:layout_width="320sp"
        android:layout_height="580sp"
        android:orientation="vertical"
        android:background="@drawable/patient_item_bidulgi"
        android:layout_marginTop="40sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">

        <Button
            android:id="@+id/patient_btn1"
            android:background="@drawable/cancel_line_background"
            android:layout_width="150sp"
            android:layout_height="45sp"
            android:textSize="30sp"
            android:fontFamily="@font/tmoney"
            android:text="취  소"
            android:layout_marginTop="530sp"
            android:layout_gravity="center_horizontal"
            />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>